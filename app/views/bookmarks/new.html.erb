<h2>Add a movie to your <%= @list.name %> list!</h2>
<br>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
